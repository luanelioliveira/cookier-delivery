environment:
  status: OK
server:
  port: 8888

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2.console:
    enabled: true
    path: /h2-console
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  profiles:
    active: test
