environment:
  status: OK
server:
  port: 8888
spring:
  datasource:
    password: ${DB_PASSWORD}
    url: ${DB_URL}
    username: ${DB_USERNAME}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  profiles:
    active: test

ryuk:
  container:
    image: testcontainersofficial/ryuk